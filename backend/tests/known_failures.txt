# 后端测试棘轮基线 —— 由 backend/scripts/check_test_ratchet.py --update 生成。
#
# 这里列出的是主干上「已知失败」的用例，绝大多数是测试与实现漂移：实现往前走了，
# 断言没跟上。它们不是被批准的缺陷，而是待裁决项——每修好一个就应该从本文件移除，
# 让基线单调收紧。禁止为了让检查通过而往这里添加新条目。
#
# 当前条目数：0
#
# 基线已清零：新的失败一律视为本次改动引入的回归，先修复再提交，
# 不要往这里添加条目。
