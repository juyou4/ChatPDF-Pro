# ==================== 按需本地 PDF 解析组件 ====================
# 由 /api/runtime/addons/local-parser/install 的受管安装器使用。
# 不要在 start.bat / start.sh 中自动安装本文件。

# 扫描 PDF OCR
pdf2image==1.17.0
pytesseract==0.3.13

# DocLayout-YOLO 图表/版面识别与权重下载
torch>=2.4,<2.5
torchvision>=0.19,<0.20
doclayout_yolo>=0.0.2
huggingface_hub>=0.20.0
opencv-python>=4.10,<5
matplotlib>=3.7
pandas>=2.0
psutil>=5.9
py-cpuinfo>=9.0
pyyaml>=6.0
requests>=2.31
scipy>=1.10
seaborn>=0.13
thop>=0.1.1
tqdm>=4.66

# OpenDataLoader 去脏与结构化表格层；仍需要系统提供 Java 11+。
opendataloader-pdf
