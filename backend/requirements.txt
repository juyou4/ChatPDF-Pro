# ==================== 核心依赖 ====================
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# 数据验证
pydantic==2.10.3
pydantic-settings==2.6.1

# HTTP客户端
httpx==0.28.1

# 环境变量
python-dotenv==1.0.1

# ==================== PDF处理 ====================
PyPDF2==3.0.1
pdfplumber==0.11.4
PyMuPDF==1.24.0  # 更强大的PDF文本提取，支持坐标级操作
Pillow>=10.0.0   # 图片处理，用于压缩提取的图片

# OCR支持 (可选，用于扫描版PDF)
pdf2image==1.17.0
pytesseract==0.3.13
# paddleocr==2.7.3  # 可选：更好的中文OCR支持，但体积较大
# paddlepaddle==2.6.1  # paddleocr依赖

# ==================== 向量检索系统 ====================
langchain==0.3.13
langchain-community==0.3.13
langchain-core==0.3.28

# 向量数据库
faiss-cpu==1.9.0.post1

# 文本向量化
sentence-transformers==3.3.1

# ==================== AI SDK ====================
openai==1.57.2
anthropic==0.40.0
google-generativeai==0.8.3

# ==================== 记忆系统增强 ====================
watchdog>=3.0.0  # 文件监听（可选，用于 Markdown 源文件同步）
