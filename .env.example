# API Configuration (Optional - Users can configure in UI)
# DEFAULT_API_PROVIDER=openai
# DEFAULT_MODEL=gpt-3.5-turbo
# DEFAULT_API_KEY=your-api-key-here

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend Configuration
FRONTEND_PORT=3000
REACT_APP_API_URL=http://localhost:8000

# Feature Flags
ENABLE_VECTOR_SEARCH=true
ENABLE_STREAMING=true
ENABLE_TABLE_EXTRACTION=true

# Paper metadata hydration (all external calls are opt-in; local metadata works offline)
# CHATPDF_ENABLE_PAPER_METADATA_HYDRATION=false
# CHATPDF_PAPER_METADATA_HYDRATION_TIMEOUT_SECONDS=8
# CHATPDF_PAPER_METADATA_UNPAYWALL_EMAIL=
# CHATPDF_PAPER_METADATA_SEMANTIC_SCHOLAR_API_KEY=
# Optional providers only run when base fields are still missing:
# CHATPDF_ENABLE_PAPER_METADATA_OPENALEX=false
# CHATPDF_ENABLE_PAPER_METADATA_ARXIV=false
# CHATPDF_ENABLE_PAPER_METADATA_OPENREVIEW=false

# Redis Configuration (Optional - for caching)
# REDIS_URL=redis://localhost:6379
# REDIS_TTL=3600

# Database Configuration (Optional - for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/chatpdf

# Storage Configuration
# UPLOAD_FOLDER=./uploads
# MAX_FILE_SIZE=50MB

# Logging
LOG_LEVEL=INFO
# LOG_FILE=./logs/app.log

# CORS Settings
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
