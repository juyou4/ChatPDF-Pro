| Round | Q | 现象 | 归因类别 | 最可能的单点 retrieval/citation 投影故障 | 关键证据 | 最小 retrieval-only 修复建议 | 需要新增的回归测试 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 36 | Q1 | live `/chat` 最终只保留 1 条 context / citation，回答还声称“只提供了 DiffuLT 的数值，缺少第二好的方法结果”。 | `检索排序差` | 最可能的单点故障在 [`embedding_service.py`](/e:/Project/Chatpdf/backend/services/embedding_service.py#L3130) 的 `_apply_numeric_table_same_bundle_hard_gate()`：对“第二好的方法 / second-best”这类 implicit comparator query，代码先把 `DiffuLT` 行选成唯一 anchor，再用包含 `table_id + dataset + backbone + column_key` 的 `item_bundle_key` 过滤 `table_row`。同表 comparator rows 一旦 `table_focus_columns` 与 anchor 不完全一致，就会被 hard gate 提前删掉；后面只剩 anchor row 及其 same-table support。`/chat` 端 [`chat_routes.py`](/e:/Project/Chatpdf/backend/routes/chat_routes.py#L1911) 到 [`chat_routes.py`](/e:/Project/Chatpdf/backend/routes/chat_routes.py#L1992)、[`chat_routes.py`](/e:/Project/Chatpdf/backend/routes/chat_routes.py#L2720) 到 [`chat_routes.py`](/e:/Project/Chatpdf/backend/routes/chat_routes.py#L2735) 只是继续投影这批结果，无法恢复已被 gate 掉的 comparator rows。 | 1. [`ragas_results_round35_numeric_subset.json`](/e:/Project/Chatpdf/backend/tests/ragas_results_round35_numeric_subset.json) 的 Q1 同时出现 `contexts_count=1`、`table_chunk_hits=2`、`numeric_chunk_hits=3`，说明不是“没召回”，而是“召回后被收口/同质化”。 2. [`test_rerank_pipeline_order.py`](/e:/Project/Chatpdf/backend/tests/test_rerank_pipeline_order.py#L1024) 和 [`test_rerank_pipeline_order.py`](/e:/Project/Chatpdf/backend/tests/test_rerank_pipeline_order.py#L2227) 已证明 second-best 查询在扩展阶段能抽出 `cRT / RIDE / ADRW`。 3. [`test_rerank_pipeline_order.py`](/e:/Project/Chatpdf/backend/tests/test_rerank_pipeline_order.py#L1392) 已证明 hard gate 会把 anchor exact row 复制到 support table/caption 上，所以 comparator rows 一旦被删，下游只会重复看到 `DiffuLT`。 4. 实测最小复现：给同一张 `Table 8` 的 `DiffuLT / cRT / RIDE / ADRW` 四行仅设置不同 `table_focus_columns`，调用 `_apply_numeric_table_same_bundle_hard_gate()` 后只剩 `DiffuLT`。 5. [`test_rerank_pipeline_order.py`](/e:/Project/Chatpdf/backend/tests/test_rerank_pipeline_order.py#L4644) 只覆盖了“显式点名 comparator”的 finalize bundle，缺少 implicit second-best 的 finalize/hard-gate 回归。 | 只改 retrieval：在 `_apply_numeric_table_same_bundle_hard_gate()` 的 implicit second-best 分支放宽 row 过滤条件。具体做法是：当 `_is_numeric_table_bundle_query(query, hints)` 为真、`explicit_comparator_mode` 为假，且 query 命中 `第二好/second-best/runner-up` 时，`table_row` 不再要求完整 `item_bundle_key == allowed_bundle_key`；改为先锁定 `allowed_table_ids`，再按 `table_id + dataset + backbone` 保留同表 comparator rows，最后依赖 `_ensure_numeric_table_evidence_slots()` 保底 `required_bundle_rows >= 4`。最小化改动点是“只放宽 implicit second-best 的 hard gate”，不动 prompt，不动 citation prompt。 | 1. 在 [`test_rerank_pipeline_order.py`](/e:/Project/Chatpdf/backend/tests/test_rerank_pipeline_order.py) 新增 `test_numeric_table_same_bundle_hard_gate_keeps_second_best_comparator_rows`：输入同表 `DiffuLT/cRT/RIDE/ADRW`，故意给不同 `table_focus_columns`，断言 hard gate 后 comparator rows 不会被过滤成只剩 anchor。 2. 在 [`test_rerank_pipeline_order.py`](/e:/Project/Chatpdf/backend/tests/test_rerank_pipeline_order.py) 新增 `test_numeric_table_finalize_without_rerank_keeps_second_best_bundle_under_page_content_pollution`：加入更高分的 `page_content` 干扰项，断言 final 仍包含 `DiffuLT + comparator rows`，且不是 `anchor row + duplicated support`。 3. 在 [`test_chat_routes_non_stream.py`](/e:/Project/Chatpdf/backend/tests/test_chat_routes_non_stream.py) 或 [`test_citation_relevance.py`](/e:/Project/Chatpdf/backend/tests/test_citation_relevance.py) 新增 single-ref 场景：当 retrieval 侧已经提供 second-best bundle，而答案只写 `[1]` 时，`retrieval_meta.context_segments` 仍应保留多条 comparator evidence，而不是只剩 anchor row。 |

```text
fix_retrieval
1. 在 implicit second-best query 上，把 `_apply_numeric_table_same_bundle_hard_gate()` 的 row 级过滤从“完整 bundle_key 精确匹配”放宽到“同 table_id / dataset / backbone 的 comparator bundle 保留”。
2. 保留现有 exact-row 向 support table/caption 的传播，但它只能作为补充；不能替代 comparator rows 本身。
3. 修复后优先复跑 Q1 以及 `second-best` / `runner-up` / `第二好的方法` 同类 numeric_table 用例，确认 final results 与 `/chat` citations 都至少保留 3-4 条同表 row evidence。

fix_prompt
1. 本题不建议先改 prompt。
2. 现象根因在 retrieval hard gate，不在 numeric_table prompt 约束；先修 retrieval，再看是否还需要 prompt 细化。
```
